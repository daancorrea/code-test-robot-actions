import headAction from "./head.action";
import leftArmAction from "./leftArm.action";
import rightArmAction from "./rightArm.action";

const allActions = {
  headAction,
  leftArmAction,
  rightArmAction,
};
export default allActions;
